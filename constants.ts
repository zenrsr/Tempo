
export enum Role {
  Owner = 'org_owner',
  Admin = 'admin',
  Approver = 'approver',
  Viewer = 'viewer',
}

export enum TaskStatus {
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
  Expired = 'expired',
}
